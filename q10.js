// function getProductNamesForOrder(orders, targetOrderId) {
//     // Initialize an empty array to hold the product names
//     let result = [];

//     // Iterate through each order in the orders array
//     for (let i = 0; i < orders.length; i++) {
//         // Check if the current order's orderId matches the targetOrderId
//         if (orders[i].orderId === targetOrderId) {
//             // If it matches, set result to the products of this order
//             result = orders[i].products;
//             // Break the loop as we have found the target order
//             break;
//         }
//     }

//     // Return the result array
//     return result;
// }
